<script>
    let urlParams = new URLSearchParams(window.location.search);
    // const version = urlParams.get('v');
    // const serial = urlParams.get('id');
    const article = urlParams.get('tr') || 'crashing-before-gtagame';

    urlParams.delete('v');
    urlParams.delete('id');
    urlParams.delete('tr');
    let remainingParams = urlParams.toString();
    if (remainingParams) {
        remainingParams = '?' + remainingParams;
    }

    window.location.href = '/sa/trouble/' + article + '/' + remainingParams;
</script>
